# Online Library - Веб-приложение

Веб-приложение для управления электронной библиотекой и закладками в книгах.

## 📋 Описание

Это простое веб-приложение, которое предоставляет функциональность для:
- **Управления книгами**: добавление, редактирование, удаление, получение списка книг
- **Управления закладками**: создание закладок с номером последней прочитанной страницы

Приложение использует:
- **Backend**: Java 21, Jetty сервер, PostgreSQL БД
- **API**: REST API с JSON форматом ответов
- **Архитектура**: Service Layer паттерн с Repository паттерном для работы с БД

## 🚀 Быстрый старт

### Требования
- Java 21 или выше
- Maven 3.6+
- PostgreSQL БД (настроенная согласно `application.properties`)

### Установка и запуск

#### 1. Клонируйте репозиторий
```bash
git clone <repository-url>
cd online_library
```

#### 2. Настройте базу данных

Отредактируйте файл `src/main/resources/application.properties`:
```properties
# Пример конфигурации
db.url=jdbc:postgresql://localhost:5432/online_library
db.user=postgres
db.password=your_password
```

#### 3. Скомпилируйте проект
```bash
mvn clean compile
```

#### 4. Соберите JAR файл
```bash
mvn package
```

#### 5. Запустите приложение
```bash
java -jar target/online_library-1.0-SNAPSHOT.jar
```

Приложение запустится на **http://localhost:8080**

## 📡 REST API

### 📚 Книги

#### Получить все книги
```http
GET /api/books
```
**Ответ:**
```json
{
  "success": true,
  "message": "Books retrieved successfully",
  "data": [
    {
      "id": 1,
      "name": "The Great Gatsby",
      "path": "/books/gatsby.pdf"
    }
  ]
}
```

#### Получить книгу по ID
```http
GET /api/books/{id}
```

#### Добавить новую книгу
```http
POST /api/books
Content-Type: application/json

{
  "name": "The Great Gatsby",
  "path": "/books/gatsby.pdf"
}
```

#### Обновить книгу
```http
PUT /api/books/{id}
Content-Type: application/json

{
  "name": "The Great Gatsby (Updated)",
  "path": "/books/gatsby.pdf"
}
```

#### Удалить книгу
```http
DELETE /api/books/{id}
```

### 🔖 Закладки

#### Получить все закладки для книги
```http
GET /api/bookmarks?bookId={bookId}
```
**Ответ:**
```json
{
  "success": true,
  "message": "Bookmarks retrieved successfully",
  "data": [
    {
      "id": 1,
      "bookId": 1,
      "lastPage": 150,
      "dateOfLastReading": "2024-12-09"
    }
  ]
}
```

#### Получить закладку по ID
```http
GET /api/bookmarks/{id}
```

#### Добавить новую закладку
```http
POST /api/bookmarks
Content-Type: application/json

{
  "bookId": 1,
  "lastPage": 150,
  "dateOfLastReading": "2024-12-09"
}
```

#### Обновить закладку
```http
PUT /api/bookmarks/{id}
Content-Type: application/json

{
  "bookId": 1,
  "lastPage": 200,
  "dateOfLastReading": "2024-12-09"
}
```

#### Удалить закладку
```http
DELETE /api/bookmarks/{id}
```

## 📁 Структура проекта

```
online_library/
├── pom.xml                                      # Maven конфигурация
├── README.md                                    # Этот файл
└── src/main/java/org/example/
    ├── Main.java                                # Точка входа
    ├── WebServer.java                           # Jetty веб-сервер
    ├── Menu.java                                # CLI меню (консольное приложение)
    ├── servlets/                                # REST API сервлеты
    │   ├── Servlet.java                         # Интерфейс сервлета
    │   ├── BookServlet.java                     # API для книг
    │   └── BookmarkServlet.java                 # API для закладок
    ├── service/                                 # Бизнес-логика
    │   ├── BookService.java                     # Интерфейс сервиса книг
    │   ├── BookServiceImpl.java                  # Реализация сервиса книг
    │   ├── BookmarkService.java                 # Интерфейс сервиса закладок
    │   └── BookmarkServiceImpl.java              # Реализация сервиса закладок
    ├── repository/                              # Доступ к данным
    │   ├── BookRepository.java                  # Интерфейс репозитория книг
    │   ├── BookDBRepository.java                # БД репозиторий книг
    │   ├── BookmarkRepository.java              # Интерфейс репозитория закладок
    │   └── BookmarkDBRepository.java            # БД репозиторий закладок
    ├── entities/                                # Модели данных
    │   ├── Book.java                            # Сущность Книга
    │   └── Bookmark.java                        # Сущность Закладка
    ├── web/                                     # Веб-компоненты
    │   └── ApiResponse.java                     # Формат ответа API
    ├── DBManager/                               # Управление БД
    │   └── PostgreSQLManager.java
    ├── Exception/                               # Пользовательские исключения
    │   └── NoEntityException.java
    ├── IO/                                      # Консольный ввод-вывод
    │   └── IO.java
    └── utils/                                   # Утилиты
        ├── Autoincrement.java
        └── DateUtils.java
```

## 🔧 Зависимости

| Зависимость | Версия | Назначение |
|-------------|--------|-----------|
| Lombok | 1.18.42 | Генерация getters/setters |
| PostgreSQL JDBC | 42.7.8 | Драйвер базы данных |
| Jetty Server | 11.0.18 | HTTP сервер |
| Jetty Servlet | 11.0.18 | Поддержка сервлетов |
| Gson | 2.10.1 | JSON обработка |
| Servlet API | 4.0.1 | Спецификация сервлетов |

## 🛠️ Разработка

### Добавление нового API endpoint

1. Создайте новый сервлет в папке `org.example.servlets`
2. Наследуйте от `HttpServlet`
3. Добавьте аннотацию `@WebServlet("/api/your-path/*")`
4. Реализуйте методы `doGet()`, `doPost()`, `doPut()`, `doDelete()`
5. Используйте `Gson` для сериализации JSON
6. Возвращайте ответы в формате `ApiResponse<T>`

### Пример:
```java
@WebServlet("/api/example/*")
public class ExampleServlet extends HttpServlet {
    private final Gson gson = new Gson();

    @Override
    protected void doGet(HttpServletRequest req, HttpServletResponse resp) 
            throws ServletException, IOException {
        resp.setContentType("application/json");
        resp.setCharacterEncoding("UTF-8");
        
        String response = gson.toJson(ApiResponse.success("Hello"));
        resp.getWriter().write(response);
    }
}
```

## 📝 Логирование

Приложение выводит информацию о ходе работы в консоль:
- ✓ Успешные операции
- ✗ Ошибки

Для более подробного логирования рекомендуется добавить SLF4J + Logback.

## 🐛 Troubleshooting

### Ошибка: "Port 8080 already in use"
Измените порт в `WebServer.java`:
```java
private static final int PORT = 8081; // или другой свободный порт
```

### Ошибка: "Cannot connect to database"
Проверьте:
1. Запущена ли PostgreSQL
2. Правильны ли параметры в `application.properties`
3. Существует ли БД `online_library`

### API возвращает 500 ошибку
Смотрите вывод консоли для деталей ошибки и исправьте запрос.

## 📄 Лицензия

Проект создан в учебных целях.

## 👨‍💻 Автор

**Разработка:** Студент 3-го курса  
**Дисциплина:** Java разработка

---

**Версия:** 1.0-SNAPSHOT  
**Последнее обновление:** 9 декабря 2024 г.
